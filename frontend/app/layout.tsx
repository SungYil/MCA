import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MCA - Global Market Analysis",
  description: "Advanced AI-powered Global Market Analysis Dashboard",
  openGraph: {
    title: "MCA - Global Market Analysis",
    description: "Real-time AI analysis of global markets and portfolio management.",
    images: ['/mca_logo.png'],
  },
  icons: {
    icon: '/mca_logo.png',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
